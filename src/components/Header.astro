
---
const navItems = [
  { href: '/programm', label: 'Visioon' },
  { href: '/minust', label: 'Minust' },
  { href: '/uudised', label: 'Blogi' },
  { href: '/toeta', label: 'Toeta' }
];
---
<header class="sticky top-0 bg-white/90 backdrop-blur z-50">
  <div class="max-w-7xl mx-auto flex items-center justify-between p-4">
    <a href="/" class="text-xl font-bold text-primary">Hannes Klaas</a>
    <nav class="space-x-6">
      {navItems.map(i => (<a href={i.href} class="hover:text-primary">{i.label}</a>))}
    </nav>
  </div>
</header>
